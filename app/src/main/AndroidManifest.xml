<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cris.hackathon.cosanostra">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".uiComponents.god.GodActivity" />
        <activity android:name=".uiComponents.medic.MedicActivity" />
        <activity android:name=".uiComponents.main.MainActivity" />
        <activity android:name=".uiComponents.room.RoomActivity"></activity>
    </application>

</manifest>